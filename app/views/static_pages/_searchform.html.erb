<%= form_with url: root_url, method: :get do |form| %>
  <div class="field">
    <%= form.label :id %>
    <%= form.text_field :id %>
  </div>

  <div class="actions">
    <%= form.submit "Search" %>
  </div>
<% end %>
